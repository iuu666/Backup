#!name=当前汇率监控
#!desc=多API汇率波动监控，支持自定义兑换基数，面板显示汇率和更新时间，支持波动提醒和系统通知
#!author=okk
#!update=2025-08-06
#!icon=bitcoinsign.circle
#!arguments=threshold:0.3,notify:true,icon:bitcoinsign.circle,color:#EF8F1C,UPDATE_INTERVAL:86400,base_amount:1
#!arguments-desc=[参数设置]\nthreshold: 汇率波动提醒阈值（百分比，默认0.3）\nnotify: 是否推送通知（true或false，默认true）\nicon: 面板图标名\ncolor: 图标颜色\nUPDATE_INTERVAL: 面板刷新间隔（秒，默认86400）\nbase_amount: 自定义兑换数量，默认1

[Panel]
当前汇率=script-name=汇率监控,update-interval={{{UPDATE_INTERVAL}}}

[Script]
汇率监控=type=generic,script-path=https://raw.githubusercontent.com/iuu666/Backup/refs/heads/main/js/HL/test.js,argument=threshold={{{threshold}}}&notify={{{notify}}}&icon={{{icon}}}&color={{{color}}}&base_amount={{{base_amount}}}


#!name=当前汇率监控
#!desc=多API监控汇率波动，支持波动提醒与系统通知推送，自定义兑换基数，面板展示详细汇率信息
#!icon=bitcoinsign.circle
#!color=#EF8F1C
#!author=okk
#!update=2025-08-06

#!arguments=threshold:0.3,notify:true,icon:bitcoinsign.circle,color:#EF8F1C,base_amount:1,UPDATE_INTERVAL:86400
#!arguments-desc=[参数设置]
threshold: 汇率波动提醒阈值（百分比，默认0.3）  
notify: 是否发送通知（true或false，默认true）  
icon: 面板图标名（默认bitcoinsign.circle）  
color: 面板图标颜色（默认#EF8F1C）  
base_amount: 兑换基数，支持小数，默认1  
UPDATE_INTERVAL: 面板刷新间隔（秒，默认86400，即一天）  

# ===== 面板配置 =====
[Panel]
# 定义面板名称和对应脚本的调用名称，刷新间隔用参数控制
当前汇率=script-name=汇率监控,update-interval={{{UPDATE_INTERVAL}}}

# ===== 脚本配置 =====
[Script]
# 脚本类型为generic，指定脚本路径（换成你实际脚本地址）
# 通过 argument 传入参数，均来自上面定义的参数设置
汇率监控=type=generic,script-path=https://raw.githubusercontent.com/iuu666/Backup/refs/heads/main/js/HL/exchange.test.js,argument=threshold={{{threshold}}}&notify={{{notify}}}&icon={{{icon}}}&color={{{color}}}&base_amount={{{base_amount}}}
